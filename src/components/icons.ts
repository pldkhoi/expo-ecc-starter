/**
 * Curated set of icons from `lucide-react-native`. Importing each icon by name
 * (rather than `import * as`) keeps Metro tree-shaking effective.
 *
 * Need an icon that is not exported here? Add a named re-export below — do not
 * import directly from `lucide-react-native` in feature code.
 */

export {
  AlertCircle,
  ArrowLeft,
  ArrowRight,
  Bell,
  Check,
  ChevronDown,
  ChevronLeft,
  ChevronRight,
  ChevronUp,
  Eye,
  EyeOff,
  Home,
  Loader,
  Lock,
  LogOut,
  Mail,
  Menu,
  Moon,
  MoreHorizontal,
  Plus,
  Search,
  Settings,
  Star,
  Sun,
  Trash2,
  User,
  X,
} from 'lucide-react-native';

export type { LucideIcon, LucideProps } from 'lucide-react-native';
